#include<stdio.h>
#include<conio.h>
void main()
{
char name={"TREE"};
int t,b,a;
char color[];
printf("Thickness:");
scanf("%d",&t);
printf(""Branches:");
scanf("%d",&b);
printf("color:");
scanf("%d",&color");
printf("angle");
scanf("%d",&a);
if(t>23)
{
if(b>20)
{
if(a<=45)
{
printf("\n angle of the tree is:");
}
printf("\n branches of tree is:");
}
printf("\n thickness of tree is:");
}
getch();
}
